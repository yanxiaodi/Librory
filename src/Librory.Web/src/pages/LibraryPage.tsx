import { PageFrame } from '@/components/shell/PageFrame'

export function LibraryPage() {
  return (
    <PageFrame
      eyebrow="Library"
      title="Family library"
      description="This page will become the family library browser and intake history. Right now it exists so navigation and theme state have a real landing target."
    />
  )
}
