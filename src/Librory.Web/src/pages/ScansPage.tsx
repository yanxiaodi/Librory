import { PageFrame } from '@/components/shell/PageFrame'

export function ScansPage() {
  return (
    <PageFrame
      eyebrow="Scans"
      title="Shelf scans"
      description="This page will hold scan sessions, candidate review, and correction flows. For now it is a placeholder page with the shared shell."
    />
  )
}
